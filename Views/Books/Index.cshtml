@model IEnumerable<LibrarySystem.Models.Book>

@{
    ViewData["Title"] = "Kitap Listesi";
}

<div class="container mt-4">

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
            ✅ <strong>Başarılı!</strong> @TempData["Message"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Kapat"></button>
        </div>
    }
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>📚 Kitap Listesi</h2>
        
        @if (User.IsInRole("admin"))
        {
            <a asp-action="Create" class="btn btn-success">➕ Yeni Kitap Ekle</a>
        }
    </div>

    <form asp-controller="Books" asp-action="Index" method="get" class="mb-3">
        <div class="input-group">
            <input type="text" name="searchString" class="form-control" placeholder="Kitap adı veya yazar ara...">
            <button class="btn btn-primary" type="submit">🔍 Ara</button>
            
            @if (ViewBag.CurrentBranchId != null)
            {
                <a asp-action="Index" class="btn btn-secondary">Filtreyi Temizle</a>
            }
        </div>
    </form>

    <table class="table table-hover table-bordered shadow-sm">
        <thead class="table-light">
            <tr>
                <th>Kitap Adı</th>
                <th>Yazar</th>
                <th>Kategori</th>
                <th class="text-center">📦 Stok</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="fw-bold">@item.Title</td>
                    <td>@item.Author</td>
                    <td>@item.Category</td>
                    
                    <td class="text-center">
                        @if (item.CurrentStock > 0)
                        {
                            <span class="badge bg-success">@item.CurrentStock / @item.TotalStock</span>
                        }
                        else
                        {
                            <span class="badge bg-danger">Tükendi</span>
                        }
                    </td>

                    <td>
                        <a asp-action="Details" asp-route-id="@item.BookId" class="btn btn-info btn-sm">Detay</a>
                        
                        @if (User.IsInRole("admin"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.BookId" class="btn btn-warning btn-sm">Düzenle</a>
                            <a asp-action="Delete" asp-route-id="@item.BookId" class="btn btn-danger btn-sm">Sil</a>
                        }
                        else
                        {
                            @if (item.CurrentStock > 0)
                            {
                                <a asp-controller="Requests" 
                                   asp-action="Create" 
                                   asp-route-bookId="@item.BookId" 
                                   class="btn btn-primary btn-sm">
                                   📖 Talep Et
                                </a>
                            }
                            else
                            {
                                <button class="btn btn-secondary btn-sm" disabled>Stok Yok</button>
                            }
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>