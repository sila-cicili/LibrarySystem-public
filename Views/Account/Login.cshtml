@{
    Layout = null; /* Bu sayfada üst menü (navbar) olmasın */
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Kütüphane Girişi</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    
    <style>
        /* 1. TAM EKRAN ARKA PLAN RESMİ */
        body {
            /* Resim yolunun doğru olduğundan emin ol */
            background: url('/image/1884669.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* 2. KARARTMA PERDESİ */
        .overlay {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-color: rgba(0, 0, 0, 0.5); /* %50 Siyahlık */
            z-index: 1;
        }

        /* 3. ORTADAKİ GİRİŞ KARTI */
        .login-card {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.6);
            width: 90%;
            max-width: 400px;
            position: relative;
            z-index: 2;
            text-align: center;
        }

        .login-header h3 {
            color: #2c3e50;
            font-weight: 700;
            margin-bottom: 10px;
        }

        /* Yuvarlak giriş kutuları */
        .form-control {
            border-radius: 50px; 
            padding: 12px 20px;
            border: 1px solid #ced4da;
            transition: all 0.3s;
        }

        .form-control:focus {
            box-shadow: 0 0 10px rgba(13, 110, 253, 0.2);
            border-color: #0d6efd;
        }

        /* Yuvarlak ve renkli giriş butonu */
        .btn-login {
            border-radius: 50px;
            padding: 12px;
            font-weight: bold;
            letter-spacing: 1px;
            background: linear-gradient(to right, #0d6efd, #0a58ca);
            border: none;
            width: 100%;
            transition: transform 0.2s;
        }

        .btn-login:hover {
            transform: scale(1.03);
            background: linear-gradient(to right, #0a58ca, #0d6efd);
        }

        /* Link Stili */
        .register-link {
            text-decoration: none;
            font-weight: 600;
            color: #0d6efd;
        }
        .register-link:hover {
            text-decoration: underline;
        }

    </style>
</head>
<body>

    <div class="overlay"></div>

    <div class="login-card">
        <div class="login-header">
            <div class="mb-2" style="font-size: 3rem;">📚</div>
            <h3>Kütüphane Sistemi</h3>
            <p class="text-muted mb-4">Hoş geldiniz, lütfen giriş yapın</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger py-2 shadow-sm rounded-pill mb-3" role="alert">
                ⚠️ @ViewBag.Error
            </div>
        }

        @if (TempData["Message"] != null)
        {
            <div class="alert alert-success py-2 shadow-sm rounded-pill mb-3" role="alert">
                ✅ @TempData["Message"]
            </div>
        }

        <form asp-action="Login" method="post">
            <div class="mb-3 text-start">
                <input type="text" name="username" class="form-control" placeholder="Kullanıcı Adı" required autocomplete="off" />
            </div>
            <div class="mb-4 text-start">
                <input type="password" name="password" class="form-control" placeholder="Şifre" required autocomplete="off" />
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-primary btn-login text-white">GİRİŞ YAP</button>
            </div>
        </form>
        
        <div class="mt-4 pt-2 border-top">
            <span class="text-muted small">Hesabın yok mu?</span>
            <br>
            <a asp-controller="Account" asp-action="Register" class="register-link fs-5">
                Hemen Üye Ol ✨
            </a>
        </div>
        <div class="mt-4 text-muted small" style="font-size: 0.75rem;">
            &copy; 2025 LibrarySystem | Güvenli Giriş
        </div>
    </div>

</body>
</html>