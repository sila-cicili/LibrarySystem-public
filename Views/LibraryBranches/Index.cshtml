@model IEnumerable<LibrarySystem.Models.LibraryBranch>

@{
    ViewData["Title"] = "Şubeler Listesi";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>🏢 Şube Yönetim Listesi</h2>
        <div>
            <a asp-controller="Map" asp-action="Index" class="btn btn-info text-white me-2">
                <i class="bi bi-map"></i> Haritaya Git
            </a>

            @if (User.IsInRole("admin"))
            {
                <a asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-lg"></i> Yeni Şube Ekle
                </a>
            }
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0 align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Şube Adı</th>
                        <th>Adres</th>
                        <th>Koordinat Durumu</th>
                        
                        @* 1. DEĞİŞİKLİK: Başlığı sadece Admin ise yazdırıyoruz *@
                        @if (User.IsInRole("Admin"))
                        {
                            <th class="text-end" style="min-width: 200px;">İşlemler</th>
                        }
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="fw-bold">@item.Name</td>
                            <td>@item.Address</td>
                            <td>
                                @if (item.Location != null && Math.Abs(item.Location.X) > 1.0)
                                {
                                    <span class="badge bg-success">
                                        ✅ Haritada Var<br>
                                        <small>@item.Location.Y.ToString("F4") / @item.Location.X.ToString("F4")</small>
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">
                                        ❌ Konum Hatalı (0,0)<br>
                                        <small>Haritada Görünmez</small>
                                    </span>
                                }
                            </td>

                            @* 2. DEĞİŞİKLİK: Sütunun içini de sadece Admin ise oluşturuyoruz *@
                            @if (User.IsInRole("Admin"))
                            {
                                <td class="text-end">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">
                                        <i class="bi bi-pencil"></i> Düzenle
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                                        <i class="bi bi-trash"></i> Sil
                                    </a>
                                </td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>